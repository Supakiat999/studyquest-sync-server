{
  "name": "studyquest-render-server",
  "version": "1.0.0",
  "private": true,
  "description": "Hosted StudyQuest sync server for Render and PostgreSQL.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/apply-hosted-patch.js && node scripts/save-reliability-patch.js && node scripts/v13-beta-patch.js && node scripts/route-alias-patch.js && node server.js",
    "check": "node --check server.js",
    "export:admin-state": "node scripts/export-admin-state.js",
    "import:admin-state": "node scripts/import-admin-state.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
